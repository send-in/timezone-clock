version: "3.9"

services:
  api:
    container_name: timezone-clock
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./core/.env
    ports:
      - "8000:8000"
    restart: unless-stopped